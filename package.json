{
  "name": "react-native-parallax-header",
  "version": "1.0.0",
  "description": "A simple react native package to achieve parallax effect on scrolling components(along with an animated flat list).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lit",
  "license": "ISC",
  "devDependencies": {
    "metro-react-native-babel-preset": "^0.56.0"
  },
  "dependencies": {
    "prop-types": "15.7.2"
  }
}
